// Export the API service instance
export { coinloreAPI } from './coinloreAPI';

// Export types from models
export type { 
  GlobalData, 
  Coin, 
  TickerResponse, 
  Market, 
  Exchange, 
  ExchangeDetail, 
  SocialStats 
} from '../models/types';
